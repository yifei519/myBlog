spring:
  thymeleaf:
    mode: HTML
  profiles:
    active: dev


comment:
  avatar: /images/cat.jpg

#配置文件防止乱码
server:
  servlet:
    encoding:
      force: true
      charset: utf-8
      enabled: true
  tomcat:
    uri-encoding: utf-8
  port: 80
